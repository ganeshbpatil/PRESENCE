# Performance Testing

Deferred to Phase 3+ as a formal load-testing discipline (Locust against sync-engine + attribution-engine), per TESTING_STRATEGY.md's phased bar — do this before agency-channel scale, not before MVP pilot. Phase 1–2 performance requirements are the NFRs in the PRD (sync <15min, AI drafting <8s p95, dashboard load <3s on 4G) verified manually/via basic monitoring, not formal load-test suites yet.
